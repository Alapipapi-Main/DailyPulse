import type { Article } from './types';

export function getNewsArticles(): Article[] {
  // In a real app, this would be an API call.
  // We're returning an empty array as placeholder news has been removed.
  return [];
}
